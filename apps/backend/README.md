# Backend Placeholder

This workspace is reserved for the future enterprise/server mode built with:

- `NestJS`
- `PostgreSQL`
- `Supabase`
- `Prisma`
- `Redis`

The mobile app already routes behavior through service interfaces so later API-backed implementations can replace the current on-device ones without redesigning the workflow UI.
